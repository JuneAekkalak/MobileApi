let mongoose,authRoutes;_647‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_647‍.w("./routes/auth",[["default",["authRoutes"],function(v){authRoutes=v}]]);require("dotenv").config();
const express = require('express')
const cors = require('cors')




const morgan = require("morgan");
const app = express()
const port = 3333

mongoose
    .connect(process.env.DATAVASE)
    .then(() => console.log("DB connected"))
    .catch((err) => _647‍.g.console.log("DB CONNECT ERROR: ", err));

// middlewares 
app.use(express.urlencoded({ extended: true }));
app.use(margan("dev"));
app.use(cors())
app.use(express.json());

app.get('/api', authRoutes);

app.listen(port, () => {
    console.log(`Example app listening at http://localhost:${port}`)
})